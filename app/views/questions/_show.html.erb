<div>
  <p>
    <div id="title"><%= @question.title %></div>
    <div id="info"><%= "submitted on %s" % [@question.created_at.strftime("%B #{@question.created_at.day.ordinalize}, %Y")] %></div>
    <div id="text"><%= @question.text %></div>
    <hr>
  </p>
    <%= render new_comment_path, question_id: @question.id %>
  <div id="direct-comments-container">
    <%= render @comments %>
  </div>
</div>
