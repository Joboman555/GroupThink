// Show right side panel
$('#questions').html("<%= j render @questions %>");
$('#questionsform').html("<%= j render 'new', link_id: @link_id %>");
$('.questioncontainer').show();

// Unbold all other links
$('a.selected').removeClass("selected");

// Bold currently selected link
$('#link-' + <%= @link_id %>).addClass("selected")
